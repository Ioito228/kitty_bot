# 🐾 AI Pet Bot — Питомец с характером в твоем Telegram

Мы разработали этот проект, чтобы превратить обычного «тамагочи» в нечто более живое и осязаемое. Наша главная цель — добавить механику, которая заставит поверить, что у Вас на сервере живет самостоятельный зверек, а не просто набор цифр и кнопок.

Главная фишка бота — тесная интеграция с **YandexGPT** для генерации осмысленных диалогов и **реальными погодными данными**, которые напрямую влияют на его поведение.

###  Попробовать в деле
 **Бот в Telegram:** [@mylitlepet_bot](https://t.me/mylitlepet_bot)
*(Просто перейдите по ссылке и нажмите `/start`, устанавливать ничего не нужно)*

---

##  В чем уникальность?

1.   Реальное окружение
    Питомец «живет» не в безвоздушном пространстве, а в Вашем городе. Мы подключили API погоды. Теперь, если за окном мороз или шторм, питомец может начать ворчать, чаще просить есть или отказываться играть, ссылаясь на плохое самочувствие.

2.   Диалоги на базе YandexGPT
    Забудьте про шаблонные фразы из 90-х. Мы дообучили модель (через системные промпты), чтобы питомец понимал свою личность, помнил, как его зовут, и общался как живой друг, а не бездушный скрипт.

3.   Оптимизация (никаких лагов)
    Работа с нейросетями — штука медленная. Чтобы бот не тормозил в моменте, мы внедрили систему кэширования. Меню статуса открывается мгновенно, а новая фраза от ИИ подгружается в фоне, не заставляя Вас ждать.

---

##  Над проектом работали

*   **Городничева Агата Сергеевна** — главный разработчик, архитектор бота, интеграция с YandexGPT и погодными API, продакшн-логика и оптимизация.
*   **Гоф Татьяна Вячеславовна** — дизайнер интерфейсов, разработка структуры базы данных, визуальное оформление и пользовательский опыт.

---

##  Что за что отвечает (структура кода)

Для тех, кто хочет заглянуть под капот и понять, как это собрано:

*   **`bot.py`** — Главный файл. Здесь живет вся логика Telegram-интерфейса: обработка команд, кнопки, навигация по меню.
*   **`pet.py`** — «Мозги» питомца. Содержит математическую модель состояний: как показатели (голод, здоровье, настроение) влияют друг на друга и меняются во времени.
*   **`ai_helper.py`** — Мост между ботом и YandexGPT. Тут прописаны промпты, которые превращают обычную LLM в конкретного персонажа с характером.
*   **`weather.py`** — Модуль для запросов к погодному API. Отвечает за то, знает ли зверек, что на улице дождь.
*   **`database.py`** — Скрипты для SQLite. Все данные сохраняются, даже если бот перезагрузится — питомец не забудет ни имени, ни города.
*   **`config.py`** — Файл с настройками и путями к ассетам (картинкам).

---

## Как это устроено внутри

*   **FSM (Конечные автоматы)** — Используем их, чтобы управлять состояниями бота. Например, чтобы четко понимать: сейчас мы просто общаемся, меняем имя или вводим город, и не путать одно с другим.
*   **Asyncio** — Бот полностью асинхронный. Есть фоновые задачи (тот самый `time_loop`), которые обновляют состояние питомца каждые 10 минут, даже если пользователь неактивен.
*   **Кэширование** — Чтобы не долбиться в API Яндекса каждую секунду за однотипными фразами, мы кэшируем ответы ИИ. Это экономит и время, и ресурсы.

---

##  Как запустить локально (если очень хочется)

Если Вы хотите развернуть такого же бота у себя:

1.  Склонировать репозиторий:
    ```bash
    git clone https://github.com/твой-профиль/твой-репозиторий.git
    ```
2.  Установить зависимости:
    ```bash
    pip install -r requirements.txt
    ```
3.  Прописать свои API-ключи (YandexGPT, погода, Telegram Bot Token) в `config.py`.
4.  Запустить бота:
    ```bash
    python bot.py
    ```

---

Проект выполнен для кейса Zuzex. Надеемся, наш зверек Вам понравится! Если есть идеи или нашли баги — будем рады обратной связи.

